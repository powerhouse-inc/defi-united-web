node_modules
.next
.turbo
dist
.env*.local
.DS_Store
*.log
.pnpm-store
.next/**
.vercel
# Codegen output is checked in so Docker builds don't need to introspect
# a live GraphQL schema. Regenerate locally with `pnpm codegen`.
# modules/__generated__/graphql/gql-generated.ts
playwright-report
test-results
tsconfig.tsbuildinfo
